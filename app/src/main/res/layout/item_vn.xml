<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/bg_library_item"
    android:padding="16dp">

    <!-- No badges (small filled circles read poorly on e-ink: low pixel density plus no
         antialiasing makes them blur into an ambiguous blob). Engine name is a small plain-text
         label under the icon instead, and there's no separate resume indicator at all;
         onVnClick's own LaunchChooserDialog already surfaces "Resume" when there's one to offer. -->
    <LinearLayout
        android:layout_width="44dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <ImageView
            android:id="@+id/icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_book"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/engineLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:gravity="center"
            android:textColor="@color/eink_text"
            android:textSize="9sp"
            android:visibility="gone" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="16dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/eink_text"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/playTime"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/eink_text"
            android:textSize="13sp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/vndbInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/eink_text"
            android:textSize="13sp"
            android:visibility="gone" />

    </LinearLayout>

    <TextView
        android:id="@+id/vnMenuButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/bg_icon_button"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center"
        android:text="@string/menu_dots"
        android:textColor="@color/eink_text"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/deleteButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/bg_icon_button"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center"
        android:text="@string/delete_glyph"
        android:textColor="@color/choice_button_text"
        android:textSize="18sp" />

</LinearLayout>
