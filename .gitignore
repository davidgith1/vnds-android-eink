*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.kotlin/

# Local sample/test content used for manual testing during development -- not needed to build
# or run the app, and not distributed with the repo.
/VNDS Example/
/Onscripter examples/
/testScreenshots/
/Examples/

# Ad hoc local dev/debugging tooling, not part of the published app.
/tools/

# Editor / assistant local state.
.claude/
*.kate-swp

# Local notes/docs (session findings, testing logs, etc.) -- not part of the published repo.
# README.md is the only markdown file that IS published.
*.md
!/README.md

# Release-signing secrets -- never commit a keystore or its passwords.
/keystore.properties
*.jks
*.keystore
